services:
  - type: web
    name: mintry-sync-api
    env: node
    buildCommand: npm install && npx tsc
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
